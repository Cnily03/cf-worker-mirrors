{
  "name": "mirror",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev --env=development",
    "deploy": "wrangler deploy",
    "deploy:pro": "cp wrangler.toml wrangler.toml.bak && cat production.toml >> wrangler.toml && wrangler deploy --env=production && mv wrangler.toml.bak wrangler.toml"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "3.60.3"
  }
}